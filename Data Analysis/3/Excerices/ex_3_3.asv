function ex_3_3()
    n = 5;
    M = 1000;
    
    lambda = 1/15;
    
    s = exprnd(1/lambda, n, M);
    
    cnt = 0;
    for sample=1:M
        s(:, sample)
        1/lambda;
        cnt = cnt + ttest(s(:,sample)', 1/lambda);
    end
    cnt
end